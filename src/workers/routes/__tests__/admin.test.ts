/**
 * Admin Routes Tests (non data-dump)
 * Data-dump endpoints were removed; this file now intentionally contains no tests.
 */
import { describe, it, expect } from 'vitest';

describe('Admin routes (placeholder)', () => {
  it('should have data-dump endpoints removed', () => {
    expect(true).toBe(true);
  });
});
